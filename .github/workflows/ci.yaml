name: Jenkins CI Trigger

on:
  push:
    branches:
      - sarra-bennour  # Branche à surveiller (ajuste si nécessaire)

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Trigger Jenkins job
        run: |
          curl -X POST "https://98d2-196-203-207-178.ngrok-free.app/job/projet-devops/build" \
          --user "admin:115858233081fdd33fa3766e6978a87d3c"
